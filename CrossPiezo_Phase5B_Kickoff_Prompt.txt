请完整读取现有 CLAUDE.md、任务书、LaTeX、reports/06–14，
再读取 `CrossPiezo_Phase5B_Benchmark_Consolidation_v0.1.md`。

当前决定：

- 保持 Benchmark Go；
- 不批准完整 PULSE；
- Phase 5A PMR 不可作为有效结论，因为 structural_ridge 在两个 in-source 任务上都差于 zero baseline；
- normalized symmetry residual 极高，必须恢复 source-native calculation frame；
- soft-mode 机制暂不成立。

你现在只执行 Phase 5B：

1. 冻结 Phase 5A；
2. 建立 Core/Extended panels；
3. 恢复 JARVIS/MP source-native tensor frames；
4. 审计 native/transport/common symmetry residual；
5. 训练至少一个真正优于 zero/mean 的 O(3)-equivariant tensor baseline；
6. 用一致统计重算 valid PMR；
7. 完成 leaderboard stress test；
8. 仅在点模型有效时做轻量 calibration；
9. 审计 raw strict cache 中是否存在完整 atom-resolved Λ；
10. 制定 version-shift 与 third-protocol 两种 adjudication 方案；
11. 输出 `reports/25_phase5b_decision.md` 后停止。

限制：

- 不修改 E:/DATA；
- 不联网，除非研究者另行批准；
- 不新增 DFT；
- 不开发完整 PULSE；
- 不直接修改 LaTeX 结果段；
- 不选择性报告最有利 PMR；
- 不用差于 zero 的模型解释 protocol/model ratio。
