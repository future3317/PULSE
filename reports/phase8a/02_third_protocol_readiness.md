# Phase 8A Third-Protocol Readiness Audit

> Config source: `configs/third_protocol_phase7c.yaml`  
> Baseline commit: `7c19a3a2401bc37b9f3dcb680c66c052d54f4c71` (Phase 7B, frozen before viewing Phase 7C results)

## 1. Summary

The third-protocol adjudication plan is **pre-registered and ready to execute**, subject to two documentation gaps noted below.

## 2. Material count and strata

The config specifies **48 materials** in **five strata**:

| Stratum | Count | Selection criteria |
|---------|-------|--------------------|
| consensus_elite | 10 | intersection of JARVIS and MP top-10% by F1/F3/F4 |
| jarvis_only_elite | 10 | top-10% by JARVIS but outside MP top-10% |
| mp_only_elite | 10 | top-10% by MP but outside JARVIS top-10% |
| consensus_low | 8 | bottom-20% by both sources, non-zero response |
| diversity_balanced | 10 | balance crystal system, atom count and P2 membership |
| **Total** | **48** | |

**Audit note:** The Phase 8A instruction text mentions ``四组各 12`` (four groups of 12). The config is the authoritative artifact and defines five strata with 10/10/10/8/10. This is not a readiness blocker, but the final third-protocol report should use the config strata to avoid confusion.

## 3. Deterministic reconstruction

- The strata are defined by ranking the **frozen P0 panel** on the frozen F1/F3/F4 scalars.
- The panel membership file exists: `artifacts/phase6a/panels/panel_membership.parquet` (92 KB).
- The exact 48 material IDs are **not** stored in the config; they must be generated by executing the selection script against the frozen panel. This is acceptable because the selection rule is deterministic, but the generated list should be written to a hash-bound manifest before any calculations begin.

## 4. Balance constraints

| Constraint | Config value | Assessment |
|------------|--------------|------------|
| Crystal systems covered | triclinic, monoclinic, orthorhombic, tetragonal, trigonal, hexagonal, cubic | OK |
| Max per crystal system | 10 | OK (does not force coverage; must be checked after selection) |
| Max per reduced formula | 3 | OK |
| P2 balance | `require_p2_balance: true` | OK, but actual balance must be verified after selection |

**Action before execution:** Run the selection algorithm and report the actual crystal-system, atom-count and P2-membership distribution of the 48 selected materials.

## 5. Frozen endpoints

The config locks the following endpoints:

- `F1_Frobenius`
- `F3_Longitudinal`
- `F4_KelvinOp`

No new endpoints are introduced. OK.

## 6. Frozen portfolio strategies

All nine strategies from Phase 7C are retained:

`jarvis_only`, `mp_only`, `average_percentile`, `borda_count`, `maximin_percentile`, `intersection_first`, `balanced_union`, `disagreement_abstention`, `minimax_oracle`.

OK.

## 7. Evaluation metrics

- `Recall@k`
- `NDCG@k`
- `minimax_recall_regret`
- `worst_source_normalized_utility`

OK.

## 8. Budget and resources

| Item | Config estimate |
|------|-----------------|
| Materials | 48 |
| Core-hours per material | 24--96 |
| Total core-hours | 1,152--4,608 |
| Max wall time | 4 weeks |
| Code | ABINIT or Quantum ESPRESSO |
| Functional | PBEsol or PBE |
| Pseudopotential | ONCV or PAW (documented) |
| k-point density | ≥ 1000 k-points per reciprocal atom |
| Plane-wave cutoff | ≥ 60 Ha (ABINIT) or equivalent |
| Tensor convention | piezoelectric stress e in C/m², full Cartesian 3×3×3, engineering Voigt order xx,yy,zz,yz,xz,xy |

**Assessment:** Resource estimate is plausible for a single investigator with access to a medium-size HPC cluster. A 4-week wall time is achievable if jobs are run in parallel.

## 9. Failure policy

```yaml
fail_policy: If a candidate fails to converge or relax, replace with the next-ranked candidate in the same stratum; document all failures and quarantined materials.
```

OK. The policy preserves stratum sizes and requires documentation.

## 10. Pre-registered outputs

The config requires:

- raw_total_piezo_tensor_per_protocol
- relaxed_structure_and_CIF
- convergence_log
- transformation_history_json
- source_field_provenance_record

All outputs support reproducibility and provenance.

## 11. No cherry-picking from Phase 7C results

- The config's `baseline_commit` is `7c19a3a2401bc37b9f3dcb680c66c052d54f4c71`, which is the Phase 7B commit.
- Phase 7C results were generated after this commit, so the strata could not have been tuned using Phase 7C outcomes.
- OK.

## 12. Gaps and recommendations

| Gap | Severity | Recommendation |
|-----|----------|----------------|
| Exact 48-material list not yet generated | Low | Generate from frozen panel, write to `results/third_protocol/candidate_list.json`, and hash-bind it before running any DFT. |
| Actual crystal-system / P2 balance not verified | Low | Add a pre-calculation balance check to the third-protocol run script. |
| No remote execution directory prepared | Low | Clone the audit branch to `/home/workspace/lrh/PULSE_phase8a` (or equivalent) and copy `artifacts/phase6a/panels/panel_membership.parquet`. |
| Resource reservation not confirmed | Medium | Confirm HPC allocation before starting; 4,608 core-hours is non-trivial. |
| Software version pinning | Low | Record exact ABINIT/Quantum ESPRESSO versions, pseudopotential files, and k-generation tools in the protocol report. |

## 13. Go/no-go for execution

**Ready to execute** once:

1. The 48 candidate IDs are generated and hash-bound.
2. A balance check confirms reasonable coverage of crystal systems and P2 membership.
3. Compute resources are reserved.
4. A run script records software versions and transformation history.

The third protocol should **not** be executed in this Phase 8A session; it is audited only.
