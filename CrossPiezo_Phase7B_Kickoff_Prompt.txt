执行 CrossPiezo Phase 7B。

基线：

ede4066921b4d22bcc5688ae91febcb429871dbb

新分支：

research/screening-resolution-v2

先读取 Phase 7A 代码、CSV、报告和 v0.4，然后读取 `CrossPiezo_Phase7B_Methodological_Upgrade.md`。

必须先复现并修复这些问题：

- AUC 未归一化；
- q_consensus 使用 pointwise first crossing；
- quantile-normalization 对 rank 指标是恒等结论；
- property-control 差值解释反向且没有真实 grouped bootstrap；
- Borda 方向、NDCG、regret、set 截断和 budget/target 定义错误；
- 阴离子字符串分类错误；
- Material Go 的独立验证不能写 `pass by construction`。

执行要求：

1. 先写 red tests；
2. 冻结 `configs/phase7b.yaml`；
3. 重算 screening resolution、controls、heterogeneity 和 robust portfolio；
4. 主实现与独立实现逐项对照；
5. 计算脚本只写 CSV/JSON，报告文本从脚本中拆出；
6. 写完整 v0.5 稿件并编译；
7. 更新 CCF-A 方法立项，但不训练模型；
8. 输出 `Q1 Material Go / Benchmark Paper Only` 后停止。

禁止新 DFT、PULSE、PMR、componentwise/O(3)、soft-mode 因果主张和修改 P0/P2。
