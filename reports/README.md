# CrossPiezo reports

## Current reports

- `CURRENT_STATUS.md`: current verification and submission state.
- `open_questions.md`: unresolved active decisions only.
- `phase7c/`: frozen screening-resolution work-package reports.
- `phase8a/`: independent takeover and submission audit.
- `phase8b/`: frozen-artifact consistency and readiness decision.
- `phase9/`: generated statistical-upgrade report and deferred third-protocol
  preflight.

## Historical reports

The numbered Phase 0–6 reports, `correctness_v1/`, `correctness_v1_1/`,
`phase6a/`, and `phase6b/` are retained for provenance. They are not current
instructions and should not be used to overwrite Version A claims.

When a generated report contains a timestamp, it records the run that produced
the artifact. Do not manually update timestamps in live documentation.
