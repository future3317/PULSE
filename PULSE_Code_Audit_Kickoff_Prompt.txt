你现在执行的是一次 correctness reset，不是下一轮科研扩展。

请首先读取：

- CLAUDE.md
- CROSSPIEZO_TASKBOOK.md
- reports/00_environment_and_scope.md 到 reports/25_phase5b_decision.md
- PULSE_Code_Audit_and_Correction_Taskbook_v0.1.md

Git 基线必须是：

ee419516f8641fba18e99266c3288349f96e2785

执行要求：

1. 从 master 创建 `audit/correctness-v1` 分支。
2. 冻结所有旧 artifacts/reports，不覆盖。
3. 将所有 Phase 0–5B 科学数字视为 provisional。
4. 先为任务书 C-01 至 C-15 写失败测试，再修复。
5. 特别优先审计：
   - piezo stress e 的 Voigt shear scaling；
   - polar rank-3 improper O(3) 变换是否错误地额外乘 det；
   - fractional symmetry matrices 是否被错误用于 Cartesian tensors；
   - source-native residual 是否实际复用了 JARVIS/common space group；
   - StructureMatcher rms/max 返回值是否反置；
   - lattice basis transform 是否被误当作 Cartesian rotation；
   - orbit discrepancy 是否遗漏 exact-frame transport；
   - longitudinal/shear ranking 是否只是坐标分量；
   - e3nn 是否 symmetrize 错轴且没有 PBC；
   - prototype split 是否其实只是元素集合；
   - source-held-out/counterfactual 标签是否与真实训练数据不符；
   - report 是否硬编码旧数字；
   - PMR normalized numerator/denominator 是否混用。
6. 使用 pymatgen trusted APIs、功共轭恒等式、Cartesian symmetry operations 和 synthetic structures 做独立 oracle。
7. 从 JARVIS/MP 各抽样至少 30 条，复现来源自身 tensor-derived scalar。
8. 所有修复后结果写入 `artifacts/correctness_v1/` 和 `reports/correctness_v1/`。
9. 生成 old-vs-corrected result table。
10. 完成 `reports/correctness_v1/10_correctness_decision.md` 后停止。

禁止：

- 修改 E:/DATA 或 ~/DATA；
- 新 DFT；
- version shift；
- third protocol；
- PULSE 开发；
- 在主 LaTeX 写结果；
- 为保住旧结论选择性修复；
- 直接 push master。

开始前先输出：

- 你对已识别风险的理解；
- 将创建的 branch；
- 准备新增的测试列表；
- 预计会失效的旧 artifacts；
- 需要人工确认的事项。

然后执行审计。
