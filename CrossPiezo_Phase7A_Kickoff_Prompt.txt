你现在执行 CrossPiezo Phase 7A。

先读取：

- CLAUDE.md
- CrossPiezo 当前任务书与 correctness/Phase 6A/6B 决策
- CrossPiezo_Invariant_Manuscript_v0.2/v0.3
- CrossPiezo_Phase7A_Screening_Resolution_and_Robust_Portfolio.md

基线 commit：

687351861862e3460994bf9bab7f8d5e734ec153

新建分支：

research/screening-resolution-v1

唯一目标：

把当前“weak cross-database concordance”升级为“数据库能识别宽泛高响应区域、但无法稳定解析 elite tail”的 screening-resolution story，并检验 property controls、电子/离子贡献和 source-robust portfolios。

按任务书依次完成 A–H。执行时：

- 先冻结配置和主 endpoint；
- 保持 573 对 P0 与 207 对 P2 不变；
- 所有新数字由脚本生成并带 hash；
- 允许定向检索文献，但只使用可靠/原始来源并保存 literature matrix；
- 不做新 DFT；
- 不恢复 PULSE、PMR、componentwise/O(3)、soft-mode 因果主张；
- 不把 two-source robustness称为物理验证；
- 不覆盖旧 LaTeX。

开始前只输出：
1. 将读取的输入；
2. 冻结的主问题和 endpoints；
3. 执行计划；
4. 预计产物；
5. 阻塞项。

完成 `reports/phase7a/07_phase7a_decision.md` 后停止。
